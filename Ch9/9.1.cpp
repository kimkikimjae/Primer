#include <iostream>

void ex_9_1() {
	// (a) maybe list will be appropriate, because according to those words that be written, they might be randomly composed in sentence
		// and this means that a lot of operations would be happening in the middle of container, rather than being on the both ends.

	// (b) deque, because operations occur only on the both end sides.

	// (c) all of them could be possible, because sorting has no relationship with the kind of containers. No matter by what type of container is composed.
}